import { createMuiTheme } from '@material-ui/core/styles';

export const myMuiTheme = createMuiTheme({
    palette: {
        primary: { 
            main: "#455a64",
        },
        secondary: { 
            main: "#8bc34a" 
        },
    },
});